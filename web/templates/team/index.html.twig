{% extends 'base.html.twig' %}

{% block title %}Teams{% endblock %}

{% block body %}
    {{ render_flash_messages() }}

    <h1>Teams</h1>

    <div class="d-flex flex-row-reverse">
        {% if is_granted("IS_AUTHENTICATED_FULLY") %}<a class="btn btn-success" href="{{ path('team_create') }}">Create new team</a>{% endif %}
    </div>

    <br>

    <div name="team-table" data-settings='{{ datatable_settings(datatable) }}'>Loading...</div>
    
{% endblock %}
